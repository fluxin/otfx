diff --git a/src/lib.rs b/src/lib.rs
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -2,6 +2,9 @@ pub mod cli;
 pub mod effects;
 pub mod engine;
 pub mod utils;
 
+// Compile-timing probe: invalidates only the ttfx crate, with no runtime effect.
+const _CACHED_INVALIDATION_PROBE: () = ();
+
 use std::sync::atomic::{AtomicBool, Ordering};
 
 static INTERRUPTED: AtomicBool = AtomicBool::new(false);
